#include <stdio.h>

int main()
{
    int principal = 100, rate = 50, time=1;
    int simpleIntrest = (principal*rate*time)/100;
    printf("Simple Intrest = %d", simpleIntrest);

    return 0;
}